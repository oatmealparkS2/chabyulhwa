# 04. TechSpec 템플릿

> 📌 소유자: 개발팀 (PM은 읽기 전용) | 용도: JIRA FE/BE/BO Sub-task 생성의 직접 소스
> 변경 이력은 Confluence 버전 히스토리로 관리한다. 인라인 날짜 태그 금지.
> 구현 구분 범례: 🟢 기존 시스템 그대로 활용 / 🟡 기존 지면 내 기능 확장 / 🔵 신규 구현 필요

```markdown
## 1. 업무 플로우
| # | 담당 | 행동 / 이벤트 | 시스템 | 구현 구분 | 관련 US |
| --- | --- | --- | --- | --- | --- |
| **▶ Step 1. {단계명}** |
| 1 |  |  |  | 🔵 신규 |  |
| **▶ Step 2. {단계명}** |
| 2 |  |  |  | 🟡 기능 확장 |  |
| **▶ Step 3. {단계명}** |
| 3 |  |  |  | 🟢 기존 |  |

## 2. API 인터페이스
### 2-1. {API명}
| 항목 | 내용 |
| --- | --- |
| Endpoint | `METHOD /api/path` |
| Request | `param1` (type, required/optional) |
| Response — 성공 | `{ key: value }` |
| Response — 실패 | `{ error: "reason" }` |
| Response — 오류 | HTTP 500, FE에서 "일시적인 오류" 처리 |

### 2-2. {API명}
| 항목 | 내용 |
| --- | --- |
| Endpoint |  |
| Request |  |
| Response |  |

## 3. 데이터 모델 (추가 / 변경 필드)
| 테이블 / 모델 | 필드명 | 타입 | 설명 |
| --- | --- | --- | --- |
|  | `field_name` | string |  |
|  | `field_name` | number |  |

## 4. 처리 시나리오 (코드 블록으로 단계 서술)
### 시나리오 1 — {시나리오명}
    1. {단계}
    2. {단계}
       - (조건 A) → {처리}
       - (조건 B) → {처리}
    3. {단계}

## 5. 계산 로직 (수식이 있는 경우에만 작성)
| 구분 | 계산식 | 예외 처리 |
| --- | --- | --- |
|  | `결과 = A × B` | — |
|  | `결과 = A - B` | 결과 < 0이면 최소값 처리 |

## 6. BO 구현 상세 (BO 변경이 있는 경우에만 작성)
### 6-1. {메뉴명} (`/path`)
* **신규 필드 / 컬럼:** {설명}
* **필터 추가:** {설명}
* **유효성 조건:** {설명}

## 7. 기타 연동 (알림톡, ERP, 외부 API 등)
* {연동 항목}: {변경 내용}
```
