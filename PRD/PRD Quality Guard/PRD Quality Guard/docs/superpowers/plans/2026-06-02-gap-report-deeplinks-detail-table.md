# GAP 리포트 v2 (딥링크 + 하이브리드 상세표) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** GAP 리포트의 위치 링크를 섹션 딥링크로, GAP 상세블록을 하이브리드 표(서술 5필드 세로 2열 표 + 처분 분리줄)로 전환한다 — 엔진 3파일 + 차별화리뷰Pro 인스턴스 2파일.

**Architecture:** 엔진(`prd-quality-guard` 스킬)의 SKILL.md·schemas.md·verification.md를 먼저 고쳐 새 규격을 박은 뒤, 5개 PRD 페이지의 실제 헤딩에서 앵커를 도출해 인스턴스 산출물(GAP·TC)을 retrofit한다. 검증은 독립 구조검증 subAgent 재디스패치 + C2 결함주입 회귀 + 딥링크 표본 디코딩.

**Tech Stack:** Markdown 산출물, Atlassian MCP(`mcp__atlassian__getConfluencePage`), Mermaid(기존 도식 불변), Agent 도구(구조검증 subAgent: general-purpose·sonnet).

**환경 주의:** 이 프로젝트는 git repo가 **아니다**. 커밋 스텝은 없다 — 모든 변경은 디스크 저장으로 완료되고, 커밋은 사용자가 직접 한다. 각 Task 말미의 "확인"은 git 대신 편집 결과 육안/grep 확인이다.

**앵커 도출 규칙 (전 Task 공통, 실측 확정):**
```
anchor = 헤딩텍스트의 공백 → "-" 치환, 비ASCII는 UTF-8 퍼센트인코딩, 점(.) 보존
deeplink = <풀URL(타이틀 슬러그 포함)>#<anchor>
폴백 = 헤딩 매칭 불확실 시 슬러그 포함 풀URL(프래그먼트 없음)
검증 예: "2-1. 리워드 체계" → #2-1.-%EB%A6%AC%EC%9B%8C%EB%93%9C-%EC%B2%B4%EA%B3%84
```

---

## 파일 구조

| 파일 | 책임 | 변경 |
|---|---|---|
| `.claude/skills/prd-quality-guard/SKILL.md` | 절차 엔진 | step1·2·5 수정 |
| `.claude/skills/prd-quality-guard/references/schemas.md` | 템플릿·스키마 | 앵커규칙 소절 신설·상세블록 템플릿 교체·주석 갱신 |
| `.claude/skills/prd-quality-guard/references/verification.md` | 구조검증 C1~C7 | C2 표행 탐지로 갱신 |
| `차별화리뷰Pro-추천인코드/doc/GAP-차별화리뷰Pro-추천인코드.md` | 인스턴스 GAP | 헤더·색인24행·상세24블록 |
| `차별화리뷰Pro-추천인코드/doc/TC-차별화리뷰Pro-추천인코드.md` | 인스턴스 TC | 헤더·본문링크3 |

실행 순서: 엔진(Task1~3) → 앵커맵(Task4) → 인스턴스(Task5~6) → 검증(Task7).

---

## Task 1: SKILL.md — step1·2·5 갱신

**Files:**
- Modify: `.claude/skills/prd-quality-guard/SKILL.md` (line 17, 20, 55)

- [ ] **Step 1: step1 L# 맵 불릿 교체 (line 17)**

old:
```
- **L# 라벨 부여**: PRD가 여러 페이지면 각 페이지에 레이어 라벨(예: L1 개요/KR · L2 정책 · L3 화면·BO · L4 TechSpec — 페이지 역할 기반, PRD마다 다름)을 부여한다. 이 L#→pageId→URL 맵을 step5 헤더 메타 `대상 PRD` 줄에 `L# · pageId · … · [링크](URL)`로 기록 → 본문 위치 약기호의 L-토큰이 페이지 단위로 하이퍼링크된다(섹션 딥링크는 앵커 불안정으로 안 함).
```
new:
```
- **L# 라벨 부여 + 딥링크 base**: PRD가 여러 페이지면 각 페이지에 레이어 라벨(예: L1 개요/KR · L2 정책 · L3 화면·BO · L4 TechSpec — 페이지 역할 기반, PRD마다 다름)을 부여한다. 이 L#→pageId→URL 맵을 step5 헤더 메타 `대상 PRD` 줄에 `L# · pageId · … · [링크](URL)`로 기록한다. **base URL은 입력 URL의 타이틀 슬러그를 떼지 말고 풀URL(`.../pages/<id>/<슬러그>`) 그대로 보존**한다 — 섹션 프래그먼트(`#앵커`)가 안정 동작하려면 슬러그 포함 정규형이 필요(Confluence가 pageId로 정규화하므로 슬러그 형태 자체는 관대).
- **섹션 딥링크 발행**: 본문 위치 약기호의 L-토큰은 **페이지 내 섹션 헤딩 앵커로 딥링크**한다. 앵커 도출 규칙 = `헤딩텍스트의 공백→"-" 치환 + 비ASCII UTF-8 퍼센트인코딩(점 보존)`, 딥링크 = `<base풀URL>#<앵커>`. 헤딩인 토큰(예 `2-1`·`US-01`)은 자기 헤딩 앵커, 표 행/하위항목 토큰(예 `AC-4`·`OI-01`·`§6`·`S1-B`)은 **가장 가까운 상위 헤딩 앵커**에 태운다. 매칭이 불확실하면 **페이지 레벨 폴백**(슬러그 풀URL·프래그먼트 없음). 앵커는 분석한 version에 고정 — 헤딩 rename 시 페이지 상단으로 폴백(치명적 아님·재실행 시 재생성).
```

- [ ] **Step 2: step2 헤딩 인벤토리 캡처 1문장 추가 (line 20 끝)**

line 20 끝(`… step 5 헤더 메타에 쓴다.`) 뒤에 이어 붙임:
```
 또한 본문에서 헤딩 라인(마크다운 `#`~`######`)을 추출해 페이지별 **헤딩 인벤토리**(`{헤딩텍스트 → 앵커}`)를 구성해 둔다 — step1 딥링크 규칙으로 위치 토큰을 헤딩 앵커에 해소하는 데 쓴다.
```

- [ ] **Step 3: step5 GAP 상세블록 규격 갱신 (line 55)**

line 55에서 `(2) **GAP별 상세블록**(...)` 부분을 다음으로 교체:
old(부분):
```
(2) **GAP별 상세블록**(위치 링크·현황/문제점 분리·PM질문·처분(유지/제외/오판정)·관련TC, 색인과 1:1)
```
new:
```
(2) **GAP별 상세블록**(색인과 1:1, **하이브리드 표** 형식 — 서술 5필드[위치·현황·문제점·PM질문/제안·관련TC]는 `| 항목 | 내용 |` 세로 2열 표, `처분`(유지/제외/오판정)은 표 밖 편집줄로 분리; 셀 내 다중문장은 `<br>`)
```
또한 같은 줄의 `위치의 L-토큰은 헤더 L#→URL 맵으로 링크.`를 다음으로:
```
위치의 L-토큰은 헤더 L#→URL 맵을 base로 **섹션 딥링크**(step1 규칙; 폴백 시 페이지 레벨).
```

- [ ] **Step 4: 확인**

Run: `grep -nE "딥링크|헤딩 인벤토리|하이브리드 표" ".claude/skills/prd-quality-guard/SKILL.md"`
Expected: step1·step2·step5에 각각 등장(최소 4 hit). "앵커 불안정으로 안 함" 문구는 0 hit.

---

## Task 2: schemas.md — 앵커규칙·상세블록 템플릿·주석

**Files:**
- Modify: `.claude/skills/prd-quality-guard/references/schemas.md` (line 33~53, 110, 115)

- [ ] **Step 1: 2단 상세블록 템플릿 교체 (현 line 41~52)**

old:
```
*2단 — GAP별 상세블록 (색인의 각 GAP-ID와 1:1):*

### GAP-NN  〈분류〉·〈심각도〉·〈기법〉
- 위치: [L# 〈섹션약기호〉](〈URL〉) … (L-토큰은 헤더 메타의 L#→URL 맵에 해소 — **페이지 단위** 링크. 섹션약기호는 링크 텍스트에 남겨 페이지 내 위치표시)
- 현황: (PRD가 실제로 말하는 사실)
- 문제점: (왜 갭인가 — 리스크/공백)
- PM 질문/제안: …
- 처분: ☑유지(기본)  ☐제외(사유: ___)  ☐오판정(사유: ___)
- 관련 TC: 〈TC-ID〉 (TC 파일 참조)  · 도식 있으면 `→ M2 도식 참조`

처분 의미 — **제외**: 갭 실재하나 PM 미반영(의도된 동작/범위 외/리스크 수용) · **오판정**: 엔진 오검출(가짜 갭). 둘 다 사유 필수. 재실행 시 결번 색인 이월(아래 "회차/버전 템플릿").
```
new:
```
*2단 — GAP별 상세블록 (색인의 각 GAP-ID와 1:1, 하이브리드 표):*

서술 5필드는 세로 2열 표, `처분`은 표 밖 편집줄로 분리(처분은 PM이 직접 체크/사유 입력 → 표 셀 편집은 파이프 정렬이 깨져 불편 + carry-forward 수확이 라인을 파싱). 셀 내 다중문장(①② 열거 포함)은 `<br>`로 줄바꿈:

### GAP-NN  〈분류〉·〈심각도〉·〈기법〉
| 항목 | 내용 |
|---|---|
| 위치 | [L# 〈섹션약기호〉](〈base풀URL〉#〈앵커〉) … (L-토큰은 헤더 L#→URL 맵 base에 step1 앵커규칙으로 **섹션 딥링크**; 매칭 불확실 시 페이지 레벨 폴백. 섹션약기호는 링크 텍스트에 남겨 위치표시) |
| 현황 | (PRD가 실제로 말하는 사실) |
| 문제점 | (왜 갭인가 — 리스크/공백) |
| PM 질문/제안 | … |
| 관련 TC | 〈TC-ID〉 (TC 파일 참조) · 도식 있으면 `→ M2 도식 참조` |

처분: ☑유지(기본)  ☐제외(사유: ___)  ☐오판정(사유: ___)

처분 의미 — **제외**: 갭 실재하나 PM 미반영(의도된 동작/범위 외/리스크 수용) · **오판정**: 엔진 오검출(가짜 갭). 둘 다 사유 필수. 재실행 시 결번 색인 이월(아래 "회차/버전 템플릿").
```

- [ ] **Step 2: "앵커 도출 규칙" 소절 신설 (3단 도식 섹션 설명 직후, 현 line 53 뒤)**

현 line 53(`… 전이표 전체·완전 매트릭스는 TC 파일).`) 뒤에 새 단락 추가:
```

**위치 링크 — 섹션 딥링크 규칙:** 위치 토큰은 헤더 메타 `대상 PRD`의 L#→URL을 base로 섹션 헤딩 앵커까지 딥링크한다.
- 앵커 = 헤딩텍스트의 공백 → `-` 치환, 비ASCII는 UTF-8 퍼센트인코딩, 점(`.`) 보존. 딥링크 = `<base풀URL>#<앵커>`.
- base URL은 입력 URL의 타이틀 슬러그를 보존한 풀URL(`.../pages/<id>/<슬러그>`).
- 헤딩이 아닌 토큰(표 행·하위항목)은 가장 가까운 상위 헤딩 앵커에 태움. 매칭 불확실 시 페이지 레벨 폴백(프래그먼트 없음).
- 검증 예: `### 2-1. 리워드 체계` → `#2-1.-%EB%A6%AC%EC%9B%8C%EB%93%9C-%EC%B2%B4%EA%B3%84`.
- **취약성:** 앵커는 분석 version에 고정 — 헤딩 rename 시 깨지고 페이지 상단으로 폴백. GAP 헤더 메타에 한 줄 캡션으로 명기.
```

- [ ] **Step 3: 헤더 메타 블록 템플릿에 캡션 + 슬러그 명기 (현 line 108~115)**

현 line 110의 `대상 PRD` 줄을 다음으로(슬러그 포함 URL 명시):
old:
```
> **대상 PRD**: L<#> · pageId <숫자> · version v<k> · lastModified <YYYY-MM-DD> · [링크](<URL>)
```
new:
```
> **대상 PRD**: L<#> · pageId <숫자> · version v<k> · lastModified <YYYY-MM-DD> · [링크](<풀URL: .../pages/<숫자>/<슬러그>>)
> _위치 링크 앵커는 위 version 기준 헤딩에 고정 — 헤딩명 변경 시 페이지 상단으로 폴백._
```

- [ ] **Step 4: 확인**

Run: `grep -nE "하이브리드|섹션 딥링크 규칙|퍼센트인코딩|version 기준 헤딩에 고정" ".claude/skills/prd-quality-guard/references/schemas.md"`
Expected: 각 1+ hit. "페이지 단위" (구 위치주석) 0 hit.

---

## Task 3: verification.md — C2 표행 탐지로 갱신

**Files:**
- Modify: `.claude/skills/prd-quality-guard/references/verification.md` (line 24~29)

- [ ] **Step 1: C2 본문 교체**

old:
```
**C2 — 갭 리포트 구조 정합 (색인 표 ↔ 상세블록 ↔ 필수필드)**
GAP 파일은 (1단) 색인 표 + (2단) GAP별 상세블록 구조다. 다음을 모두 확인:
- 색인 표 6열 헤더(`GAP-ID | 위치 | 기법 | 분류 | 심각도 | 한줄요약`)가 존재한다.
- 색인 표의 모든 GAP-ID에 대응 상세블록(`### GAP-NN …`)이 **1:1**로 존재한다(색인엔 있는데 블록 없음=FAIL, 블록은 있는데 색인에 없음=FAIL).
- 각 상세블록의 필수 필드 **위치·현황·문제점·PM 질문/제안**이 모두 비어있지 않다(`처분`·`관련 TC`는 비어도 무방). 분류·심각도·기법은 블록 헤딩 `### GAP-NN  분류·심각도·기법`에서 확인.
하나라도 어긋나면 FAIL. 증거: 색인 GAP-ID 수, 1:1 불일치 ID 목록, 결손 필드 GAP-ID와 어느 필드인지.
```
new:
```
**C2 — 갭 리포트 구조 정합 (색인 표 ↔ 상세블록 ↔ 필수필드)**
GAP 파일은 (1단) 색인 표 + (2단) GAP별 상세블록(하이브리드 표) 구조다. 다음을 모두 확인:
- 색인 표 6열 헤더(`GAP-ID | 위치 | 기법 | 분류 | 심각도 | 한줄요약`)가 존재한다.
- 색인 표의 모든 GAP-ID에 대응 상세블록(`### GAP-NN …`)이 **1:1**로 존재한다(색인엔 있는데 블록 없음=FAIL, 블록은 있는데 색인에 없음=FAIL).
- 각 상세블록은 `| 항목 | 내용 |` 세로 2열 표를 갖고, 필수 필드 행 **위치·현황·문제점·PM 질문/제안**이 모두 존재하며 `내용` 칸이 비어있지 않다(`관련 TC` 행은 비어도 무방). `처분`은 표 밖 별도 라인(`처분: ☑유지 …`)으로 존재하며 비어도 무방. 분류·심각도·기법은 블록 헤딩 `### GAP-NN  분류·심각도·기법`에서 확인.
하나라도 어긋나면 FAIL. 증거: 색인 GAP-ID 수, 1:1 불일치 ID 목록, 결손/빈 필드 행 GAP-ID와 어느 필드인지.
```

- [ ] **Step 2: 확인**

Run: `grep -n "세로 2열 표\|표 밖 별도 라인" ".claude/skills/prd-quality-guard/references/verification.md"`
Expected: C2 내 등장.

---

## Task 4: 앵커맵 구축 — 5페이지 헤딩 인벤토리

**Files:**
- (산출물 없음 — 작업 메모리상 `{L# · pageId · 슬러그 · [헤딩텍스트→앵커]}` 맵 구성)

- [ ] **Step 1: 4페이지 추가 페치**

L2(2082799647)는 이미 확보. 나머지 4페이지를 `mcp__atlassian__getConfluencePage`(contentFormat: markdown)로 페치:
- L1 `1931739326`, L3-화면 `2083160088`, L3-BO `2083454980`, L4 `2082897936`

- [ ] **Step 2: 헤딩 추출 + 앵커 도출**

각 페이지 본문에서 `#`~`######` 헤딩 라인을 추출. 각 헤딩에 규칙 적용 → `{헤딩텍스트 → 앵커}`. 슬러그는 인스턴스 CLAUDE.md "대상 PRD" URL의 슬러그 그대로:
- L1 → `OP202605_+Pro`
- L2 → `01.+_+Pro`
- L3-화면 → `02-1.+Pro+_+Pro`
- L3-BO → `02-2.+BO+_+Pro`
- L4 → `03.Tech+Spec_+Pro`

- [ ] **Step 3: 위치 토큰 ↔ 헤딩 매핑표 작성**

GAP 색인 24행 + 상세 24블록 + TC 본문 3링크에 등장하는 모든 위치 토큰을 열거하고, 각 토큰을 헤딩(자기/상위) 또는 폴백으로 분류한 매핑표를 만든다. 예:
```
GAP-01 위치 "L2 2-1·OI-01" → L2 base + #<2-1 앵커>  // OI-01은 표행→2-1 상위헤딩? 아니면 "4. Open Items" 앵커
GAP-01 위치 "L4 §2·5"      → L4 base + #<§2 헤딩 앵커 또는 폴백>
```
헤딩이 모호한 토큰(`§6`·`S1-B`·`미결2`)은 가장 가까운 상위 헤딩 또는 폴백으로 명시 확정.

- [ ] **Step 4: 확인**

매핑표에 GAP 24건·TC 3링크의 모든 L-토큰이 빠짐없이 등장하고, 각 항목이 (앵커 또는 폴백) 둘 중 하나로 분류됐는지 육안 확인.

---

## Task 5: GAP 파일 retrofit

**Files:**
- Modify: `차별화리뷰Pro-추천인코드/doc/GAP-차별화리뷰Pro-추천인코드.md`

- [ ] **Step 1: 헤더 메타 — base URL 슬러그 복원 + 캡션**

`대상 PRD` 5줄의 `[링크](.../pages/<id>)` → `[링크](.../pages/<id>/<슬러그>)`(Task4 슬러그). 5줄 직후에 한 줄 추가:
```
> _위치 링크 앵커는 위 version 기준 헤딩에 고정 — 헤딩명 변경 시 페이지 상단으로 폴백._
```

- [ ] **Step 2: 색인 표 24행 위치 셀 딥링크화**

색인 표(section 1) 각 행 `위치` 셀의 모든 `[…](base)` 링크를 Task4 매핑표의 `base#앵커`(또는 폴백)로 치환. 링크 텍스트(약기호)는 불변.

- [ ] **Step 3: 상세블록 24개 → 하이브리드 표**

각 `### GAP-NN …` 블록의 불릿 6줄을 하이브리드 표로 변환. GAP-01 예시(완성형):
```
### GAP-01  모순·High·상태전이
| 항목 | 내용 |
|---|---|
| 위치 | [L2 2-1·OI-01](https://smartfoodnet.atlassian.net/wiki/spaces/platformdepartment/pages/2082799647/01.+_+Pro#2-1.-%EB%A6%AC%EC%9B%8C%EB%93%9C-%EC%B2%B4%EA%B3%84) ↔ [L4 §2·5](<Task4 L4 매핑>) |
| 현황 | L2는 추천인 리워드 "10,000원 쿠폰(최소주문 10만원)"으로 확정([기인지] OI-01 완료)인데, L4 TechSpec Step5·시나리오4는 여전히 "[TBD: 포인트 or 쿠폰]". |
| 문제점 | 배치 구현 소스가 두 값을 가리킴 — L2 확정값과 L4 TBD가 불일치. |
| PM 질문/제안 | 쿠폰으로 확정인지? L4 TechSpec의 TBD를 쿠폰으로 갱신 필요. 쿠폰 발급 API·유효기간·최소주문 10만원 조건 명시 요청. |
| 관련 TC | US04-T04 · → M2 도식 참조 |

처분: ☑유지  ☐제외(사유: ___)  ☐오판정(사유: ___)
```
변환 규칙(24블록 일괄): `- 위치:`→`| 위치 |…|`, `- 현황:`→`| 현황 |…|`, `- 문제점:`→`| 문제점 |…|`, `- PM 질문/제안:`→`| PM 질문/제안 |…|`, `- 관련 TC:`→`| 관련 TC |…|`. `- 처분:` 라인은 표 밖으로 빼서 `처분:` 단독 라인. 셀 내용에 줄바꿈 필요 시 `<br>`. 위치 셀 링크는 Step2와 동일한 딥링크.

- [ ] **Step 4: 확인**

Run: `grep -c "^| 위치 |" "차별화리뷰Pro-추천인코드/doc/GAP-차별화리뷰Pro-추천인코드.md"`
Expected: 24 (상세블록 24개 모두 표 위치행).
Run: `grep -c "^처분:" "차별화리뷰Pro-추천인코드/doc/GAP-차별화리뷰Pro-추천인코드.md"`
Expected: 24.
Run: `grep -c "pages/[0-9]*/[^)]*#" "차별화리뷰Pro-추천인코드/doc/GAP-차별화리뷰Pro-추천인코드.md"`
Expected: 딥링크(앵커 포함) 다수(폴백 제외한 헤딩 매칭 수).

---

## Task 6: TC 파일 retrofit

**Files:**
- Modify: `차별화리뷰Pro-추천인코드/doc/TC-차별화리뷰Pro-추천인코드.md`

- [ ] **Step 1: 헤더 메타 5줄 base 슬러그 복원**

`대상 PRD` 5줄 `[링크](.../pages/<id>)` → 슬러그 포함 풀URL(Task4). (TC 헤더엔 앵커 캡션 불필요 — 헤더 링크는 페이지 단위; 캡션은 GAP에만.)

- [ ] **Step 2: 본문 위치 하이퍼링크 3개 딥링크화**

- 129행 US07-T06: `[L2 정책](.../pages/2082799647)` → L2 base + 해당 섹션 앵커(첫달50%/리워드체계 = `2-1` 또는 US-07 헤딩; Task4 매핑 확정).
- 162행 US04-T04: `[L2 정책](.../pages/2082799647)` → L2 OI-01/2-1 앵커; `[L4 TechSpec](.../pages/2082897936)` → L4 해당 섹션 앵커.
표 셀 내 비링크 토큰(S1-B 등)은 불변.

- [ ] **Step 3: 확인**

Run: `grep -c "pages/[0-9]*/[^)]*#" "차별화리뷰Pro-추천인코드/doc/TC-차별화리뷰Pro-추천인코드.md"`
Expected: ≥3 (본문 딥링크 3개; 헤더 5줄은 앵커 없음).

---

## Task 7: 검증 + 마무리

**Files:**
- (검증·문서 갱신)

- [ ] **Step 1: 구조검증 subAgent 재디스패치**

`references/verification.md` "프롬프트 본문"의 `{{TC_FILE_PATH}}`·`{{GAP_FILE_PATH}}`를 두 인스턴스 산출물 절대경로로 치환해 Agent 도구(subagent_type: general-purpose, model: sonnet)로 디스패치.
Expected: C1~C6 PASS · C7 N/A(1회차). verdict 가공 없이 사용자에게 전달.

- [ ] **Step 2: C2 결함주입 회귀**

GAP 파일 사본에서 임의 상세블록 1개의 `| 현황 |` 행을 삭제 → 검증 subAgent 재디스패치 → **C2 FAIL**로 해당 GAP-ID·결손필드 정확 지목 확인. 확인 후 사본 폐기(원본 불변).

- [ ] **Step 3: 딥링크 표본 디코딩 대조**

GAP-01 등 대표 3건의 위치 딥링크 `#앵커`를 URL-decode해 Task4 헤딩 인벤토리의 실제 헤딩텍스트와 일치하는지 대조.

- [ ] **Step 4: 문서·메모리 갱신**

루트 `CLAUDE.md` 「현황」에 본 작업 항목 추가(딥링크+하이브리드 표, 검증 결과). 메모리 `prd-quality-guard-project.md`에 한 줄 갱신. 커밋은 사용자가 직접(이 repo는 git 아님 — 안내만).

---

## Self-Review

**Spec coverage:** 스펙 §3.1 엔진 3파일 = Task1~3 · §3.2 retrofit 2파일 = Task5~6 · 앵커 데이터 확보 = Task4 · §4 검증 3종 = Task7. 누락 없음.
**Placeholder scan:** Task4·5·6의 `<Task4 매핑>` 표기는 placeholder가 아니라 Task4가 산출하는 결정론적 데이터 참조(앵커 규칙이 고정이라 실행 시 기계적으로 확정). GAP-01은 완성형으로 제시. 그 외 23블록은 동일 변환규칙(Step3) 적용.
**Type consistency:** 앵커 규칙·base 슬러그·폴백 용어가 Task1~6 전반 일관. 검증 grep 패턴 일관(`pages/[0-9]*/[^)]*#`).
